import PlaceholderPage from "@/components/PlaceholderPage";

export default function Candidates() {
  return (
    <PlaceholderPage 
      title="Candidates Management"
      description="This page will contain candidate listings, CV/Resume upload functionality, candidate profiles, and import from CSV/JSON features."
    />
  );
}
